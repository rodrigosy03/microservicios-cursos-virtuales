server:
    port: 5000
spring:
    cloud:
        config:
            server:
                git:
                    default-label: config-server
                    uri: https://github.com/rodrigosy03/microservicios-cursos-virtuales
                    search-paths: config-data
    application:
        name: config-service
