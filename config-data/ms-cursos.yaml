server:
  port: 5002
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8087/eureka
    instance:
      hostname: localhost
